# Server (Vercel Environment Variables)
NEON_DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"
# Alternatively, if your code expects DATABASE_URL you can set it too:
# DATABASE_URL="..."

# Cloudinary (server-side only; never expose secrets to Vite client)
CLOUDINARY_URL="cloudinary://API_KEY:API_SECRET@CLOUD_NAME"
# or set individually if you prefer:
# CLOUDINARY_CLOUD_NAME=""
# CLOUDINARY_API_KEY=""
# CLOUDINARY_API_SECRET=""

# Admin protection for write endpoints (send as Bearer token from a private admin tool, not from the public site)
ADMIN_TOKEN="replace-with-a-long-random-string"
